# Seguridad supply chain — DiamanteAI
# Bloquea lifecycle scripts de dependencias (previene ataques tipo Axios 2026)
ignore-scripts=true

# Nunca resolver dependencias nuevas sin revision explicita del lockfile
frozen-lockfile=true

# Rechazar paquetes sin integridad SHA en el lockfile
strict-peer-dependencies=true
